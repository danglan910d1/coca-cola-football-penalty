// UserApp/src/components/Theme.js
export const Theme = {
  colors: {
    primary: '#F40009',          // Đỏ tươi classic Coca-Cola
    primaryDark: '#B30006',      // Đỏ sậm cho viền 3D của nút bấm
    background: '#f1f5f9',       // Nền sáng nhạt
    card: 'rgba(255, 255, 255, 0.85)', // Hộp kính mờ trắng (frosted glass)
    cardBorder: 'rgba(244, 0, 9, 0.25)', // Viền đỏ nhạt mờ
    textTitle: '#1e293b',        // Slate 800 cho chữ tiêu đề chính
    textSub: '#475569',          // Slate 600 cho chữ phụ
    gold: '#ffd700',
    error: '#dc2626',            // Đỏ lỗi đậm hơn
    white: '#FFFFFF',
    inputBg: 'rgba(241, 245, 249, 0.8)', // Nền input xám sáng mờ
  },
  radius: {
    input: 12, // Bo góc 12px (rounded-xl) theo mẫu
    button: 50, // Bo góc tròn dài (pill shape)
    card: 32,  // Bo góc card 32px
  }
};
