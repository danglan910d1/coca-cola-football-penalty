// GameApp/src/config/appConfig.js
/**
 * Đọc từ shared/env.js qua alias @shared
 * Metro config đã map: @shared → G:\Football Penalty\shared\
 *
 * ĐỔI GIÁ TRỊ TRONG shared/env.js — không cần sửa file này.
 */
export { getUserAppURL, default as ENV } from '@shared/env';
